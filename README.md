

Qdrant - used for storing data alongside a embeded vector representation,
        we run the service using a docker image 


all-MiniLM-L6-v2 - sentence-transfomer model ,maps an input to a 384 dim 
                    vector.